// Study domain public surface. The sub-features (`decks`, `session`, `stats`)
// each keep their own barrel; this one exposes what composes them.
export { default as StudyView } from './views/StudyView';
