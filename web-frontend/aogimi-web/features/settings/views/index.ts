// Barrel for `features/settings/views`: import several of its modules from one place.
// Files inside this folder import their siblings by path, never through here.

export { default as CreditsView } from './CreditsView';
export { default as HelpView } from './HelpView';
