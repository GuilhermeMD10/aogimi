// The `study` sub-feature's public surface — reviewing the cards the sky is
// made of. Its own two parts (`session`, `stats`) each keep a barrel; this one
// exposes the screen that composes them. The deck vocabulary it reads belongs
// to the sibling `stage` sub-feature, not here.
export { default as StudyView } from './views/StudyView';
