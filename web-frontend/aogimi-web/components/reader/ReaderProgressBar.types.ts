/** Props for `ReaderProgressBar`. */
export type ReaderProgressBarProps = {
  /** Filled fraction, 0–100. */
  fraction: number;
  /** Right-to-left layout (manga / vertical Japanese). */
  rtl?: boolean;
  /** Layout classes for the outer wrapper — width, flex, max-width, height. */
  className?: string;
};
