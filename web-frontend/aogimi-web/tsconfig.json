{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.asynciterable", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "**/*.mts"
  ],
  "//exclude": "`scripts/` holds standalone verification harnesses run directly by Node's type stripper (`node scripts/verify-fsrs.mts`). That resolver needs the literal `.ts` extension on its imports, which `allowImportingTsExtensions` would have to be on for tsc to accept — and turning that on affects the whole app. The scripts are not part of the build, so they are simply not tsc's business.",
  "exclude": ["node_modules", "scripts"]
}
