// DeepL feature flag.
//
// Set to `true` to re-enable the DeepL translation popup + its access
// points (context-menu entry in both default and Stamp readers, popup
// render in TextReaderBody). The underlying code is intentionally kept
// intact so re-enabling is a single edit.
//
// See `DEEPL.md` at the web-frontend root for the full inventory of
// files involved in the feature.

export const DEEPL_ENABLED = false;
