// DeepL feature flag.
//
// Set to `true` to re-enable the DeepL translation popup + its access
// points (selection-menu entry, popup render). The underlying code is
// intentionally kept intact so re-enabling is a single edit.
//
// See `DEEPL.md` at the repo root for the full inventory of files
// involved in the feature.

export const DEEPL_ENABLED = false;
